<h1>Articles!</h1>


<ul>
      <% @articles.each do |article| %>

    <li>
        <a href="/articles/<%=article[0]%>"><%= "Title: " + article[0] + "." %></a>
        <a href="/articles/<%=article[1]%>/<%=article[2]%>"> <%= "Description:" + article[1]+ "." %> <%= "URL:" + article[2]+ "." %></a>
    </li>

      <% end %>
    </ul>

    <h4 class = 'new'><a href="/articles/new">Add A New Article</a></h4>
